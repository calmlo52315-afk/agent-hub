ADR-010 模型路由策略
决策
模型选型暂不硬编码指定，仅定义选型原则，根据实际可用模型动态适配。

选型原则（核心规则）
1. Coding Agent：优先选择代码生成能力最强的大模型；
2. Review Agent：优先选择逻辑推理、语义理解能力最强的大模型；
3. Artifact Agent：优先选择调用成本低、响应快的模型。
边界约束
模型切换不改动业务流程、输入输出格式。

演进路径
后续落地模型负载均衡、自动降级、多模型混合路由。