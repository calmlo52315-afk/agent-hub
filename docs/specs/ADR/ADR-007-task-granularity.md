ADR-007 Task 任务粒度规范
决策
严格限制任务粒度，禁止超大任务，确保 Orchestrator 可正常拆解、Agent 可稳定执行。

背景与选择原因
任务过大易导致理解偏差、上下文过载、执行超时，必须提前划定上下限。

允许粒度（示例）
- 开发登录页面
- 开发 Todo 业务接口
- 审核指定模块代码
- 生成指定文件单测
禁止粒度（强约束）
- 开发整套系统
- 一次性完成全项目重构
- 跨数十个文件的无边界超大任务
边界约束
1. 单个任务原则上对应 ≤3 个物理文件；
2. 超大需求由 Orchestrator 强制拆分为多个细粒度子任务串行执行。
演进路径
后续增加任务自动拆分算法，支持复杂需求智能拆解。